import Phaser from 'phaser';

export default class InteriorScene extends Phaser.Scene {
  constructor() {
    super('InteriorScene');
  }

  create(data: { houseId: string }) {
    // TODO — Track C
  }
}
