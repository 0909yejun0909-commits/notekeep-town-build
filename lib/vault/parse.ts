import type { Room, Region } from '@/lib/types';

export function roomSize(room: Room): [number, number] {
  // TODO
  return [10, 10];
}

export function regionSize(region: Region): [number, number] {
  // TODO
  return [40, 40];
}
